name: Hello World

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          
      - name: test-case
        run: echo "Hello World"
      - name: Install deps
        run: |
          pip install pytest pymongo python-dotenv

      #- name: Run tests on twin file
        #run: pytest twin/test_twin_sim.py
        
      - name: Run tests on app.py
        run: pytest dashboard/tests/app_test.py

